= link_to "Back to Pictures", pictures_url

%h1 Add a Picture 

= form_for @picture do |f|
	%ul
		%li #{f.label "artist"} #{f.text_field "artist"}
		%li #{f.label "title"} #{f.text_field "title"}
		%li #{f.label "url"} #{f.text_field "url"} 
		%p= f.submit "Add!"
			
			
		